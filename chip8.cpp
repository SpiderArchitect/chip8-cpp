#include "chip8.hpp"
#include <iostream>
#include <cstring>
#include <fstream>
// fonts are stored in interpereter area of chip8 memory
uint8_t font[(0xF+1) * 5] = {
    0xF0, 0x90, 0x90, 0x90, 0xF0, // 0
    0x20, 0x60, 0x20, 0x20, 0x70, // 1
    0xF0, 0x10, 0xF0, 0x80, 0xF0, // 2
    0xF0, 0x10, 0xF0, 0x10, 0xF0, // 3
    0x90, 0x90, 0xF0, 0x10, 0x10, // 4
    0xF0, 0x80, 0xF0, 0x10, 0xF0, // 5
    0xF0, 0x80, 0xF0, 0x90, 0xF0, // 6
    0xF0, 0x10, 0x20, 0x40, 0x40, // 7
    0xF0, 0x90, 0xF0, 0x90, 0xF0, // 8
    0xF0, 0x90, 0xF0, 0x10, 0xF0, // 9
    0xF0, 0x90, 0xF0, 0x90, 0x90, // A
    0xE0, 0x90, 0xE0, 0x90, 0xE0, // B
    0xF0, 0x80, 0x80, 0x80, 0xF0, // C
    0xE0, 0x90, 0x90, 0x90, 0xE0, // D
    0xF0, 0x80, 0xF0, 0x80, 0xF0, // E
    0xF0, 0x80, 0xF0, 0x80, 0x80  // F
};

Chip8::Chip8() : I(0), SP(0), DT(0), ST(0), PC(0x200)
{
    memcpy(&memory[fontStart], font, sizeof(font));
    memset(V, 0, sizeof(V));
    memset(display, 0, sizeof(display));
    memset(stack, 0, sizeof(stack));
}

void Chip8::LoadROM(const char * filename) {
    std::ifstream file(filename, std::ios::binary | std::ios::ate);
    if(!file.is_open())
    {
        std::cerr<<"Error opening file"<<std::endl;
        return;
    }
    int size = file.tellg();
    if(size > (0xFFF - 0x200 + 1))
    {
        std::cerr<<"ROM size too large"<<std::endl;
        return;    
    }
    file.seekg(0, std::ios::beg);
    file.read((char*)&memory[0x200], size);
}
